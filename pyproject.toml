[project]
name = "system-story-coverage-demo"
version = "0.1.0"
description = "A small public demo of flow-based schema coverage."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
